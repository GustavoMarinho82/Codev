#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int contagem(long long int x, int y){
	/* insert your code here */
}

int main(){
	long long int x;
	int y;

	while(scanf("%lld %d", &x, &y) > 0){
	    printf("%d\n", contagem(x, y));
	}
	return 0;
}